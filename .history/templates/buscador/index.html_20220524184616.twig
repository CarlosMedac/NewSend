{% extends 'base.html.twig' %}

 

    {% block body %}
    

    <div class="input-group mb-3">
     <div class="input-group-prepend">
        <span class="input-group-text" id="basic-addon1">@</span></div>
       {{ form_start(formulario, {'attr': {'id': 'formBUSQUEDA','class': 'bi bi-search'}})}}
       <label for="mensaje_imagen">
           <figure class="subir_img">
              <i class="bi bi-search"></i>
           </figure>
       </label>
     
       {{ form_end(formulario) }}
  
        <div class="usuariosEncontrados">
            {% if test != " "%}

                {% include 'buscador/barraBusqueda.html.twig' %}      

            {% endif %}

        </div>
    </div>
    {% for test in test %}
        <div class="text-center" id="usuariosEncontrados">
            ola?
        </div>
    {% endfor %}
{% endblock %}
