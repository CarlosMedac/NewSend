{% extends 'base.html.twig' %}

 

    {% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
       {{ form_start(formulario, {'attr': {'id': 'formBUSQUEDA','placeholder': 'Buscar'}})}}
        
       {{ form_end(formulario) }}
    
        <div class="usuariosEncontrados">
            {% if test != " "%}

                {% include 'buscador/barraBusqueda.html.twig' %}      

            {% endif %}

        </div>
    </div>
    {% for test in test %}
        <div class="text-center" id="usuariosEncontrados">
            ola?
        </div>
    {% endfor %}
{% endblock %}
